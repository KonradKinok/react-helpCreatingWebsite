import { AsynchronicReduxApp } from "../../components/Redux/asynchronicRedux/AsynchronicReduxApp";
const Redux_Asynchronic = () => {
    return (
        <>
            <h1>Redux Asynchronic</h1>
            <AsynchronicReduxApp />
        </>
    );
};

export default Redux_Asynchronic;